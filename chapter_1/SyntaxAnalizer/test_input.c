#include <stdio.h>

int main() {
    int x = 10;
    
    if (x > 0 {
        printf("x is positive\n");

    int arr[5] = {1, 2, 3, 4, 5]];
    
    for (int i = 0; i < 5; i++) {
        printf("%d ", arr[i]);

    printf("Hello, world);
    
    // Unclosed single quote
    char c = 'a;
    
    // Unclosed multi-line comment
    /* This is a comment that starts but does not close
    
    return 0;
}

// Extra closing brace
}
